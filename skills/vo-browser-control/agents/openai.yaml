interface:
  display_name: "Virtual Office 浏览器状态"
  short_description: "读取 VO 共享浏览器状态、标签页和当前控制者"
  default_prompt: "Use $vo-browser-control to inspect the Virtual Office shared browser status, tabs, and controller without taking unsupported browser actions."
