interface:
  display_name: "Virtual Office 工作准则"
  short_description: "拼接本地 VO 地址并加载实例权威工作流"
  default_prompt: "Use $vo-operating-guidelines to detect the local Virtual Office endpoint, load its authoritative skill entrypoint, and route this task safely."
